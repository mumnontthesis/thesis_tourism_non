# คัดลอกเป็น .env แล้วใส่ค่าจริง (อย่า commit .env)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=tourism_nonthaburi
DB_PORT=3306
PORT=5000

# URL ของ frontend บน Vercel (คั่นด้วย comma ถ้ามีหลาย domain)
CORS_ORIGIN=https://your-app.vercel.app
